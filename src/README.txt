To run the program correctly, the script must be executed from the same directory as the config file (that is the src).
Otherwise, make sure you input correctly the paths inside the configuration file.
